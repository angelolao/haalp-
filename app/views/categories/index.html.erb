CATEGORIES LIST:

<% @categories.each do |t|%>
<ul>
  <li>id: <%= t.id %></li>
  <li>title: <%= t.title %></li>
  <li>description: <%= t.description %></li>
  <%= link_to "Edit", edit_category_path(id: t.id) %>
</ul>
<% end %>
